Перебор с возвратом (метод ветвей и границ) - можно решить задачу о выводе всех *** чисел методом полного перебора (брутфорсом).
Но можно отсекать лишние ветви, если у нас имеются некоторые условия.

Генерация перестановок (перестановки - permutations).


Умножение Карацубы.


Быстрые рекуррентные сортировки:
    1) Тони Хоара (quick sort):
        Сортирующее действие делается до ухода в рекурсию.
        Берётся барьерный элемент, остальные сравниваются с ним и разпределяются по 2-м подмассивом.
        Соритровка плоха тем, что при выборе "плохого" барьерного элемента сортировка замедляется. В наихудшем случае: О(n^2).
        Это вероятностный алгоритм, вероятностная оценка: W(n*log_2(n)).
    2) Сортировка слиянием (merge sort):
        Сортирующее действие (слияние) делается после ухода в рекурсию. В наихудшем слуае: O(n*log_2(n)).